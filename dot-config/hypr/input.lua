hl.config({
	input = {
		kb_layout = us,
		-- kb_options = caps:swapescape -- changed in kb settings

		follow_mouse = 1, -- 2
		scroll_factor = 1.0,

		sensitivity = 0, -- -1.0 - 1.0, 0 means no modification.
		-- force_no_accel = true,
		numlock_by_default = true,
		repeat_delay = 300,
		-- touchpad {
		--     natural_scroll = false
		-- }
	}

})
